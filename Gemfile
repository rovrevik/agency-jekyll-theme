source 'https://rubygems.org'

# Use github-pages instead of jekyll directly so we have confidence tht it will be fine when
# push to the gh-pages branch.
# gem 'jekyll', '~>2.4' # what github pages is on
# gem 'jekyll', '~>2.5.3' # latest
gem 'github-pages'
